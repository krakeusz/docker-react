language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t krakeusz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true krakeusz/docker-react npm run test -- --coverage